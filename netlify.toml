[build]
  command = "npm run build"
  publish = "build"

# (Optional) If your client output is at build/client only, publish = "build/client" also works.
# With the function wrapper, either is fine.

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200


