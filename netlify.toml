[build]
  command = "npm run build"
  publish = "build/client"

[functions]
  directory = ".netlify/functions-internal"

# ✅ Tell Netlify to send ALL paths to the Remix server function
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200

